# Gambas Form File 3.0

{ Form Form
  MoveScaled(0,0,52,8)
  { Timer1 #Timer
    #MoveScaled(1,1)
    Enabled = True
    Delay = 100
  }
  { Label1 Label
    MoveScaled(16,1,23,5)
    Font = Font["Bitstream Charter,Bold,+4"]
    Text = ("Pensando...")
    Alignment = Align.Center
  }
}
